version: "3.9"
services:
  api:
    build: .
    ports: ["8080:8080"]
    environment:
      OLLAMA_HOST: host.docker.internal
      OLLAMA_PORT: 11434
    extra_hosts:
      - "host.docker.internal:host-gateway"

  frontend:
    build: ./client/web-react
    ports: ["5173:80"]
