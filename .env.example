# CogniScribe Configuration
# Copy this file to .env and customize as needed

# ========================================
# Audio Storage Settings
# ========================================

# Base directory for all data storage (optional)
# Default: platform-specific (~/Library/Application Support on macOS, etc.)
# COGNISCRIBE_DATA_DIR=/path/to/data

# Directory for uploaded audio files (optional)
# Default: {COGNISCRIBE_DATA_DIR}/audio_storage
# AUDIO_STORAGE_DIR=/path/to/audio_storage

# Directory for temporary processed audio (optional)
# Default: {COGNISCRIBE_DATA_DIR}/temp_processed
# TEMP_AUDIO_DIR=/path/to/temp_processed

# Number of days to retain audio files (1-365)
AUDIO_RETENTION_DAYS=7

# ========================================
# File Upload Limits
# ========================================

# Maximum file upload size in MB (1-10000)
MAX_FILE_SIZE_MB=1000

# Maximum chunk size for processing in MB (1-100)
MAX_CHUNK_MB=8

# ========================================
# Security
# ========================================

# Enable PHI (Protected Health Information) detection
# Rejects uploads containing potential patient data
# STRONGLY RECOMMENDED: Keep this enabled for safety
PHI_DETECTION_ENABLED=true

# ========================================
# Audio Enhancement (Optional)
# ========================================

# Enable DeepFilterNet audio enhancement
# Requires separate installation: pip install deepfilternet
DEEPFILTERNET_ENABLED=false

# Path to DeepFilterNet binary (if not in PATH)
DEEPFILTERNET_BIN=deep-filter

# Path to custom DeepFilterNet model (optional)
# DEEPFILTERNET_MODEL=/path/to/model

# Use postfilter for better quality (recommended)
DEEPFILTERNET_USE_POSTFILTER=true

# ========================================
# Whisper Transcription
# ========================================

# Whisper model size: tiny, base, small, medium, large-v3
# Larger = more accurate but slower
# Recommendation: base (good balance), small (better accuracy)
WHISPER_MODEL=base

# Use GPU acceleration for Whisper (requires CUDA)
# Significantly faster if you have a compatible NVIDIA GPU
USE_GPU=false

# ========================================
# Ollama LLM
# ========================================

# Ollama host (hostname or full URL)
# For Docker: use service name (ollama)
# For local: use localhost
OLLAMA_HOST=localhost

# Ollama port
OLLAMA_PORT=11434

# Ollama model name
# Options: llama3.1:8b, llama2:7b, mistral:7b, etc.
# Run 'ollama list' to see installed models
OLLAMA_MODEL=llama3.1:8b

# Ollama request timeout in seconds (30-3600)
# Increase for very long lectures or slower systems
OLLAMA_TIMEOUT=300

# ========================================
# Logging
# ========================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ========================================
# CORS (Web API)
# ========================================

# Comma-separated list of allowed CORS origins
# Add your frontend URLs here
CORS_ALLOW_ORIGINS=http://localhost,http://127.0.0.1,http://localhost:5173,http://127.0.0.1:5173,tauri://localhost,app://localhost,http://tauri.localhost,https://tauri.localhost

# Allow credentials in CORS requests
CORS_ALLOW_CREDENTIALS=false

# ========================================
# API Metadata (Optional)
# ========================================

API_TITLE=CogniScribe API
API_VERSION=1.0.0
API_DESCRIPTION=Audio transcription and summarization for medical/nursing students. Educational use only.

# ========================================
# Database (Optional - Not Yet Implemented)
# ========================================

# PostgreSQL database URL (if using database features)
# DATABASE_URL=postgresql://user:password@localhost:5432/cogniscribe

# ========================================
# Authentication (Optional - If Enabled)
# ========================================

# Enable authentication
# COGNISCRIBE_AUTH_ENABLED=false

# JWT secret key (generate with: openssl rand -hex 32)
# JWT_SECRET_KEY=your-secret-key-here

# JWT algorithm
# JWT_ALGORITHM=HS256

# Access token expiry in minutes
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# ========================================
# Redis Cache (Optional - If Enabled)
# ========================================

# Redis URL for caching
# REDIS_URL=redis://localhost:6379/0
